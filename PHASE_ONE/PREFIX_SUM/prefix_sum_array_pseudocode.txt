> Input: Arr[] => Original array
> Output: Arr[] => Prefix Sum array

FUNCTION prefix_sum_array(List[int]) -> List[int]:
    IF LIST IS EMPTY:
        RETURN
    
    GET LENGTH OF ORIGINAL Array
    # CREATE A NEW ARRAY OF SAME SIZE AS ORIGINAL ARRAY
    prefix_sum_array = [0] * len(ORIGINAL ARRAY)
    # INPUT THE FIRST ELEMENT OF ORIGINAL ARRAY TO NEW ARRAY
    prefix_sum_array[0] = List[0]
    # ITERATE THE ORIGINAL ARRAY
    FOR i in range(l, len(ORIGINAL ARRAY)):
        # MAKING SURE THE NEXT ELEMENT IS THE SUM OF THE PREVIOUS ELEMENT AND
        # NEXT ELEMENT
        prefix_sum_array[i] = prefix_sum_array[i - 1] + List[i]
        prefix_sum_array.append(prefix_sum)

    RETURN prefix_sum_array